<table>
  <tr>
    <th>Booking date</th>
    <th>Location</th>


  </tr>
  <% @bookings.each do |booking| %>
    <tr>
      <td><%= booking.slot.date %></td>
      <td><%= booking.slot.property.address %></td>
      <td><%= booking.buyer.name %></td>

    </tr>
  <% end %>
</table>
